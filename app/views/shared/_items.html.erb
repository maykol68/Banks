    <%= link_to t('.Login'), new_user_session_path, class: "mx-2 text-teal-500 font-bold " unless current_user %>
    <%= link_to t('New bank'), new_bank_path, class: "text-white px-4 py-2 rounded-2xl drop-shadow-sm hover:bg-lime-300 bg-emerald-600 mx-3" if current_user %>
    <%= link_to 'New supplier', new_supplier_path, class: "text-white px-4 py-2 rounded-2xl drop-shadow-sm hover:bg-lime-300 bg-emerald-600 mx-3" if current_user %>
    <%= link_to t('.register'), new_user_registration_path,class: "mx-2 text-teal-500 font-bold " unless current_user %>
    <%= link_to t('.logout'), destroy_user_session_path , data:{
    turbo_method: :delete,
    turbo_confirm: t('common.confirm')
    },class: "mx-2 text-teal-500 font-bold " if current_user%>